grpcio
protobuf
grpcio-reflection
grpcio-status
# PyTorch — default PyPI wheels ship CUDA runtime libs, so the box is GPU-capable
# without a CUDA base image (same convention as the lightglue box).
torch>=2.0.0
numpy
opencv-python-headless

# the UniMatch package itself is vendored into the image at build time
# (git clone of autonomousvision/unimatch, pinned commit — see docker/Dockerfile)
